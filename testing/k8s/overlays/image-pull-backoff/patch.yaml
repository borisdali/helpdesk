apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres
  namespace: helpdesk-test
spec:
  template:
    spec:
      containers:
        - name: postgres
          image: postgres:99.99-nonexistent
