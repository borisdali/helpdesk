{
  "_comment": "Demo infrastructure — points at the demo-postgres container started by the demo profile.",
  "db_servers": {
    "demo-db": {
      "name": "Demo PostgreSQL (local container)",
      "connection_string": "host=demo-postgres port=5432 dbname=postgres user=postgres password=demopassword sslmode=disable",
      "tags": ["demo"],
      "approval_override_roles": ["dba_lead"]
    }
  }
}
