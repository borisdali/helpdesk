{{/*
This template is a reminder / documentation placeholder.
The API key Secret must be created out-of-band before installing the chart:

  kubectl create secret generic helpdesk-api-key \
    --from-literal=api-key=YOUR_API_KEY

The secret name and key are configurable via values.yaml:
  model.apiKeySecret  (default: helpdesk-api-key)
  model.apiKeyKey     (default: api-key)
*/}}
