version: "2"

linters:
  settings:
    errcheck:
      exclude-functions:
        # database/sql iteration and cleanup — errors unactionable in defer/early-return
        - (*database/sql.Rows).Close
        - (*database/sql.DB).Close
        - (*database/sql.Stmt).Close
        - (*database/sql.Tx).Rollback
        # file and network close — errors in defer are unactionable
        - (*os.File).Close
        - (net.Conn).Close
        - (*net.TCPConn).Close
        - (*net.UDPConn).Close
        # archive/compression writers — flushed in defer, errors can't be recovered
        - (*compress/gzip.Writer).Close
        - (*archive/tar.Writer).Close
        # HTTP server lifecycle
        - (*net/http.Server).ListenAndServe
        - (*net/http.Server).ListenAndServeTLS
        - (*net/http.Server).Shutdown
        # JSON encoding to http.ResponseWriter — connection drops are unactionable
        - (*encoding/json.Encoder).Encode
        # Diagnostic / formatting writes and scans
        - fmt.Fprint
        - fmt.Fprintf
        - fmt.Fprintln
        - fmt.Print
        - fmt.Println
        - fmt.Sscanf
        - fmt.Sscan
        - (*text/tabwriter.Writer).Flush
        # Filesystem cleanup in defer: best-effort, not actionable on error
        - os.Remove
        - os.RemoveAll
